## Bluetooth Settings
## https://zmk.dev/docs/troubleshooting/connection-issues#unreliableweak-connection
CONFIG_ZMK_BLE=y

# [중요 수정] 신호 강도를 높여 끊김을 방지합니다. (기존 유지)
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

# [중요 수정] 실험적 기능 활성화 (기존 유지)
CONFIG_ZMK_BLE_EXPERIMENTAL_FEATURES=y

# [핵심 수정 ⭐] PIN 번호 입력을 'n'(No)로 변경했습니다.
# 이제 연결 시 번거로운 숫자 입력 없이 바로 연결됩니다.
CONFIG_ZMK_BLE_PASSKEY_ENTRY=n

## Power Management Config
## https://zmk.dev/docs/config/power
# CONFIG_ZMK_IDLE_TIMEOUT=30000
# CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=600000

## Queue limit
## https://zmk.dev/docs/keymaps/behaviors/macros#behavior-queue-limit
CONFIG_ZMK_BEHAVIORS_QUEUE_SIZE=512

## [Gemini 추가 설정]
## 1. ZMK Studio 활성화 (웹에서 실시간 키맵 변경 가능)
CONFIG_ZMK_STUDIO=y

## 2. 마우스 기능 활성화 (트랙볼 사용을 위해 명시)
CONFIG_ZMK_MOUSE=y
